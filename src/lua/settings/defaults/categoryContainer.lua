local NAME, ADDON = ...

ADDON.settings = ADDON.settings or {}

ADDON.settings.categoryContainer = {
    padding = 5,
    spacing = 3,
    maxHeight = 450,
    sortFunction = 'default',
    closeVisible = false,
    closeSize = 25,
}

