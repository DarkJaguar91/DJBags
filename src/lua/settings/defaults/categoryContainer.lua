local NAME, ADDON = ...

ADDON.settings = ADDON.settings or {}

ADDON.settings.categoryContainer = {
    padding = 5,
    spacing = 3,
    formatter = 'vertical',
    closeVisible = false,
    closeSize = 25,
}

